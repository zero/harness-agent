{
  "name": "@harness-agent/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@harness-agent/core": "workspace:*",
    "@harness-agent/tools": "workspace:*",
    "@hono/node-server": "^1.19.6",
    "hono": "^4.10.7",
    "tsx": "^4.21.0"
  }
}
